import { DomainPendingNotice } from '@/components/shell/DomainPendingNotice';

export default function TalentPage() {
  return <DomainPendingNotice title="Talent" description="Talent pipeline and recruiter tooling are a separate domain build, not part of this Platform Shell pass." />;
}
