import { DomainPendingNotice } from '@/components/shell/DomainPendingNotice';

export default function CreateGroupPage() {
  return <DomainPendingNotice title="Create Group" description="Group creation is a separate domain build, not part of this Platform Shell pass." />;
}
