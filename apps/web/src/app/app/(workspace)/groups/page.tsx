import { DomainPendingNotice } from '@/components/shell/DomainPendingNotice';

export default function GroupsPage() {
  return <DomainPendingNotice title="Groups" description="Groups are a separate domain build, not part of this Platform Shell pass." />;
}
