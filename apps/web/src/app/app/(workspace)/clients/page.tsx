import { DomainPendingNotice } from '@/components/shell/DomainPendingNotice';

export default function ClientsPage() {
  return <DomainPendingNotice title="Clients" description="Client account management is a separate domain build, not part of this Platform Shell pass." />;
}
