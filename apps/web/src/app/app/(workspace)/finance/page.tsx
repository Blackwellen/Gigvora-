import { DomainPendingNotice } from '@/components/shell/DomainPendingNotice';

export default function FinancePage() {
  return <DomainPendingNotice title="Finance" description="Billing and invoicing are a separate domain build, not part of this Platform Shell pass." />;
}
