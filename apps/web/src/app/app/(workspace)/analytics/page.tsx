import { DomainPendingNotice } from '@/components/shell/DomainPendingNotice';

export default function AnalyticsPage() {
  return <DomainPendingNotice title="Analytics" description="Workspace analytics/reporting is a separate domain build, not part of this Platform Shell pass." />;
}
