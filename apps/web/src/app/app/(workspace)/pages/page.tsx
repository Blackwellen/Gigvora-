import { DomainPendingNotice } from '@/components/shell/DomainPendingNotice';

export default function PagesPage() {
  return <DomainPendingNotice title="Pages" description="Company/topic Pages are a separate domain build, not part of this Platform Shell pass." />;
}
