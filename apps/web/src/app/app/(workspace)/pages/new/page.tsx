import { DomainPendingNotice } from '@/components/shell/DomainPendingNotice';

export default function CreatePagePage() {
  return <DomainPendingNotice title="Create Page" description="Page creation is a separate domain build, not part of this Platform Shell pass." />;
}
